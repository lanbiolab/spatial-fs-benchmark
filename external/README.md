# External Code

This repository includes a minimal vendored helper from
`theislab/atlas-feature-selection-benchmark` because several plotting scripts
source:

```text
external/atlas-feature-selection-benchmark/analysis/R/plotting.R
```

The original README and license are preserved in
`external/atlas-feature-selection-benchmark/`.
